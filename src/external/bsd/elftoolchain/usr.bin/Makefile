# $NetBSD$

SUBDIR+=	ar
SUBDIR+=	brandelf
SUBDIR+=	common
SUBDIR+=	cxxfilt
SUBDIR+=	elfcopy
SUBDIR+=	elfdump
SUBDIR+=	findtextrel
SUBDIR+=	readelf
SUBDIR+=	size
SUBDIR+=	strings

.include <bsd.subdir.mk>
